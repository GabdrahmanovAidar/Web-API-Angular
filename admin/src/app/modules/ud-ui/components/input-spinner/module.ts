import { NgModule } from "@angular/core";
import { CommonModule } from "@angular/common";
import { UDInputSpinner } from "./component";

@NgModule({
  imports: [CommonModule],
  declarations: [UDInputSpinner],
  exports: [UDInputSpinner]
})
export class UDInputSpinnerModule {
}
