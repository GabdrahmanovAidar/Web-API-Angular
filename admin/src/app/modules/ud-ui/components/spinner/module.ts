import { NgModule } from "@angular/core";
import { CommonModule } from "@angular/common";
import { UDSpinner } from "./component";

@NgModule({
  imports: [CommonModule],
  declarations: [UDSpinner],
  exports: [UDSpinner]
})
export class UDSpinnerModule {
}
